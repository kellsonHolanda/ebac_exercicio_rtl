import styled from 'styled-components';

export const ListaDeVagasContainer = styled.div`
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20px;
  justify-content: center; /* Centraliza as colunas */
`;